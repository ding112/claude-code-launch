## Claude Code 安装向导手动验收清单

### A. 通用检查
- [ ] 启动应用后，能看到 3 步向导（环境检测 / 安装执行 / 安装验证）和实时日志面板。
- [ ] 点击“复制日志”可成功复制当前日志内容。
- [ ] 状态文案能区分成功、失败、未实现（notImplementedYet）。

### B. Windows 成功路径（优先验证）
- [ ] 环境检测返回 `powershell`、`local_script`、`winget`、`git_for_windows` 项。
- [ ] 本地脚本存在时，安装先尝试 `scripts/windows/install_claude_code.ps1`。
- [ ] 本地脚本失败时，自动降级 `winget install Anthropic.ClaudeCode`。
- [ ] `winget` 也失败时，自动降级官方脚本安装。
- [ ] 安装执行期间界面保持可交互（可滚动日志、切换窗口），无“卡死”现象。
- [ ] 安装日志中可看到策略进度事件（`local_ps1` / `winget` / `remote_ps1`）。
- [ ] 安装成功后执行 `claude --version` 与 `claude doctor`，页面显示验证结果。
- [ ] `winget` 进度输出期间，实时日志区域不出现挤压其它卡片或布局错乱。
- [ ] 含颜色控制符（ANSI）的日志在页面中可读，不出现乱码控制字符。
- [ ] 含回车覆写（`\r`）的进度日志仅显示最终可读文本，不形成字符墙。

### C. Windows 失败路径
- [ ] 本地脚本缺失时，安装结果包含明确失败原因（脚本不存在或 powershell 不可用）。
- [ ] 所有策略失败时，页面显示“所有安装策略均失败”，并能从日志中看到每次尝试输出。
- [ ] `claude` 不存在时，验证失败文案提示检查 PATH 和安装状态。
- [ ] `claude doctor` 启动失败时，错误摘要明确区分“启动失败 / 执行失败(退出码) / 执行超时”。
- [ ] 安装命令超时时，失败摘要包含超时时长与命令上下文。

### D. Linux / macOS 占位路径（本期不实现）
- [ ] 在非 Windows 平台执行安装，返回 `notImplementedYet` 状态。
- [ ] 页面提示“入口已保留，本期未实现”，不误显示为安装失败。
- [ ] 日志中包含平台占位提示信息。

### E. 日志面板稳定性（回归）
- [ ] 缩窄窗口宽度后，页面主体布局保持稳定，横向滚动只出现在日志/输出容器内部。
- [ ] 点击“复制日志”后，复制内容包含原始日志文本（用于排障），不丢失关键信息。
